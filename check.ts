const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { const invoices = await prisma.hms_invoice.findMany({ orderBy: { created_at: 'desc' }, take: 3 }); console.log('INVOICES:'); invoices.forEach(i => console.log(i.invoice_number + ' - status: ' + i.status + ' - total: ' + i.total + ' - outstanding: ' + i.outstanding + ' - created_at: ' + i.created_at.toISOString())); const shift = await prisma.hms_cash_shift.findFirst({ orderBy: { created_at: 'desc' } }); console.log('SHIFT:'); console.log('start_time: ' + shift.start_time.toISOString()); } check().finally(() => prisma.$disconnect());
